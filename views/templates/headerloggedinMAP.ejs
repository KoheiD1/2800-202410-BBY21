<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CodeCrypt</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" type="text/css" href="/healthbar.css">
    <link rel="icon" type="image/png" href="favicon.png">
</head>
<body class="<%= overflowVariable %>">
  <div class="pt-4 pb-4 pr-1 relative z-10 sticky top-0 bg-white bg-opacity-40">
    <div class="pl-2">
        <div class="flex justify-start">
          <form action="/">
            <input type="submit" class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded mr-4"
              value="Back to Main Menu">
          </form>
        </div>
      </div>
    <div class="flex justify-end items-center -mt-10">
        <% if (gameStarted) { %>
        <div class="flex items-center mr-4">
            <img src="goldCoin.png" class="w-6 h-6">
            <span class="ml-1 text-gray-900"><%= playerCoins %></span>
        </div>
        <% } %>
        <img class="h-12 w-12 md:h-14 md:w-14 lg:h-16 lg:w-16 rounded-full bg-white border-2 border-gray-500 hover:border-black cursor-pointer" src="<%= userProfilePic %>" alt="Profile Picture" onclick="window.location.href='/profile'">
    </div>
</div>
