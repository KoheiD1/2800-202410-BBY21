<% let string = item.type %>
<div class=" justify-center p-5 inline-grid" onclick='showDialog(<%- JSON.stringify(item) %>)'>
    <div class='flex justify-center font-bold text-xl align-middle m-auto'>
        <img src="<%= item.type %>.png" alt="<%= item.type %>" class="">
    </div>
</div>