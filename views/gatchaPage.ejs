<%- include("templates/headerloggedin", { overflowVariable: '' }) %>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">

<div class="grid-container">
    <!-- SVG image for the background -->
    <img src="./easterEgg/gatchaMachine.svg" alt="Gatcha Machine" class="background-image">
</div>
<button onclick="window.location.href='/easteregganimation'"
        class="mt-16 px-6 py-2 bg-transparent text-black rounded-lg block mx-auto start-font text-2xl md:text-3xl lg:text-4xl xl:text-5xl animate-bounce-slow hover:text-red-600">Spin Gatcha
    </button>

<style>
    .grid-container {
        position: relative;
        display: grid;
        place-items: center;
        height: 50vh;
    }

    .background-image {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        object-fit: contain;
        z-index: 0;
    }

    @font-face {
    font-family: 'titlefont';
    src: url('startgame.ttf') format('truetype');
    }
  
    .title-font {
    font-family: 'titlefont', sans-serif;
    }
  
    @keyframes bounce-slow {
      0%, 100% {
        transform: translateY(0);
      }
      50% {
        transform: translateY(-10px);
      }
    }
  
    .animate-bounce-slow {
      animation: bounce-slow 3s infinite;
    }
  
    @font-face {
    font-family: 'startfont';
    src: url('titlefont.ttf') format('truetype');
    }
  
    .start-font {
    font-family: 'startfont', sans-serif;
    }
</style>

<%- include("templates/footer") %>

